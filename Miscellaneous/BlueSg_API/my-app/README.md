# BlueSG API

This repo contains all code for BlueSG API Test.

## Requirements
* Node 19
* Yarn

## To Run The Program
Run `yarn` in the project's root directory to install node modules

## To Start Expo Server
Run `yarn start` to run the dev server